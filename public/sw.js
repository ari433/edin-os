self.addEventListener("push",event=>{let data={};try{data=event.data?event.data.json():{}}catch{data={body:event.data?.text()}};event.waitUntil(self.registration.showNotification(data.title||"EDIN OS",{body:data.body||"Koha per planin tend 💪",icon:"/icon.svg",badge:"/icon.svg",tag:"edin-reminder",data:{url:data.url||"/"}}))});self.addEventListener("notificationclick",event=>{event.notification.close();event.waitUntil(clients.matchAll({type:"window",includeUncontrolled:true}).then(list=>{for(const c of list){if("focus"in c)return c.focus()}return clients.openWindow(event.notification.data?.url||"/")}))});